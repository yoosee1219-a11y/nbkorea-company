# Default Language
# Set the default language for the site
# Options: ko, en, vi, th, fil, uz, ne, mn, id, my, zh-CN, ru
# Default: ko (Korean)
VITE_DEFAULT_LANGUAGE=ko

# Hide Language Switcher
# Set to 'true' to hide the language switcher (for single-language deployments)
# Default: not set (language switcher visible)
# VITE_HIDE_LANGUAGE_SWITCHER=true

# For Vietnamese-only site deployment, use:
# VITE_DEFAULT_LANGUAGE=vi
# VITE_HIDE_LANGUAGE_SWITCHER=true
